[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd server && npm install", "cd client && rm -rf node_modules package-lock.json && npm install"]

[phases.build]
cmds = ["cd client && npx vite build"]

[start]
cmd = "cd server && node index.js"
